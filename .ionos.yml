---
distFolder: _site
setup:
- name: Setup project
  action: ruby/setup-ruby@v1
  properties:
    ruby-version: 2.7
    bundler-cache: true
build:
  name: Build project
  environment:
    JEKYLL_ENV: production
  command: bundle exec jekyll build
